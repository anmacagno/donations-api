en:
  activerecord:
    errors:
      models:
        credit_card:
          attributes:
            number:
              invalid: 'has an invalid format (16 digits)'
            expiration:
              invalid: 'has an invalid format (mm/yyyy)'
            cvv:
              invalid: 'has an invalid format (3 digits)'
  notifications_mailer:
    gratitude:
      subject: 'Gratitude'
      greeting: 'Hello %{email}!'
      content: 'Thank you very much for your donation.'
